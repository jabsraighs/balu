<form method="post" action="{{ path('front_user_quote_line_delete', {'id': quote_line.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ quote_line.id) }}">
    <button class="btn">Delete</button>
</form>
