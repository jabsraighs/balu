<form method="post" action="{{ path('app_admin_company_delete', {'id': user.id}) }}" onsubmit="return confirm('Are you sure you want to delete this comapny?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="btn">Delete</button>
</form>
